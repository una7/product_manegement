.wrapper
  .background_image_3
    .main_header
      = link_to "Product manegementS",root_path, class: 'top_page_link'
    = form_tag  ages_search_ages_path, method: :get, class:'use_icon'  do
      %input{:placeholder => " 検索する", :type => "search",name: "keyword"}
        -# %button{:type => "submit"}
    -# .search
    -#   検索結果
    .result
      - @ages.each do |age|
        .result__search
          = age.age + ":"     #検索したアルファベットの表示
          = age.season + " :"
          -# = age.url
          -# = link_to "ヨウジ",age.url
          -# = raw Rinku.auto_link((h(age.url)), mode=:all, link_attr='target="_blank"', skip_tags=nil)


    .yohji_link
      = link_to "ヨウジヤマモト FASHON PRESS","https://www.fashion-press.net/collections/brand/42", class:"yohji", target: "_blank"
    .search_img
      = image_tag 'misyakeike.jpg', class:"forest", size:'720x480'
